Normally the =countify library (../../AddinCfy/libCountifyQuantLibAddin.la) is called by the =countify server, not by a C++ executable such as the one in this directory.

The program here can be used to debug the lib by loading it up and calling in to it.