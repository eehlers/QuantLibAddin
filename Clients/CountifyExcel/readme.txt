- Ensure that your =countify addin is configured correctly and pointing to the desired server.

- Select the DOS batch file for the desired environment.  Edit the batch file to set the correct paths for your system.  Invoke the batch file, it should start up a new Excel instance, load the =countify addin, and the chosen demo.

- Go to the =countify store and purchase the necessary addin.

- Back in the spreadsheet, hit F9 and wait for the updates to percolate.